/*
There is no lambda functionality in this component. This component is an SQS quueu which is connected to the `crm-workflow-contact-change` lambda,
The /deploy folder is present to ensure that vdeploy doesn't reject this component as being of an invalid 'lambda' type
*/